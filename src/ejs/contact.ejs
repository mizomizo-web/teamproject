<!DOCTYPE html>
<html lang="ja">
  <%- include('./common/_head') %>
  <body>
		<% var location = {page_id:'contact'} %>

    <!-- ヘッダー -->
    <%- include('./project/_p-header') %>

    <!-- 下層ファーストビュー -->
    <%- include('./project/_p-sub-fv', {page: location}) %>

    <section class="p-sub-contact l-sub-contact">
      <div class="p-sub-contact__inner l-inner">
        <!-- パンくずリスト -->
        <div class="p-sub-contact__breadcrumb">
          <%-include('./component/_c-breadcrumb',{
            categoryName:'お問い合わせ',
            postName:'',
            src:'contact.html'
            })
          %>
        </div>

				<div class="p-sub-contact__container">
					<%- include('./project/_p-sub-contact-list', {page: location}) %>
				</div>
      </div>
    </section>

    <!-- フッター -->
    <%- include('./project/_p-footer') %>

    <!-- ページトップ -->
    <%- include('./component/_c-pagetop') %>
  </body>
</html>